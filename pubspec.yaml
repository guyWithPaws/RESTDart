name: RESTDart
description: A web server built using the shelf package.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  postgres: ^2.2.0
  shelf_router: ^0.7.2
  args: ^1.6.0
  shelf: ^0.7.5

dev_dependencies:
  pedantic: ^1.9.0
